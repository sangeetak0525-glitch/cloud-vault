/**
 * Backend API URL for hosted frontends (GitHub Pages, static hosting).
 * Leave empty when the UI and API are on the same origin (localhost, Render full deploy).
 *
 * Example after deploying backend to Render:
 *   window.CLOUDVAULT_API_URL = 'https://cloudvault-xxxx.onrender.com';
 */
window.CLOUDVAULT_API_URL = '';
